---
- hosts: localhost
  connection: local
  gather_facts: no
  roles:
    - ansible-download-directory
    - usr-local-src-directory
    - ssh-keys
    - passwordless-sudo
    - python3
    - git_pre_commit
